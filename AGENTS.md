# Exam Trainerプロジェクト指示

## 目的

Exam Trainerを、型安全で試験に依存しないローカルファーストPWAとして構築する。v1はバックエンド、ログイン、有料サービス、有料AI APIなしで利用可能にする。

## 正式な基準

- 人が最後に承認した工程成果物を基準とする。
- 最後に承認されたゲートは`docs/00_開発管理/approval-ledger.md`で確認する。
- 人が現在の工程を明示的に承認するまで、次のウォーターフォール工程を開始しない。
- 要件、設計、実装、試験を変更した場合は、`docs/00_開発管理/requirements-traceability.md`も同期する。
- 未解決のリスクと判断は`docs/00_開発管理/risk-issue-register.md`へ記録し、会話だけに残さない。

## 製品制約

- 共通UIと各エンジンをOSS-DB、AWS、その他の特定試験に依存させない。
- 試験は`exam-packs/<exam-id>/exam.yaml`、`domains.yaml`、`questions/*.md`を通じて追加し、共通コードで特定のPackを特別扱いしない。
- 問題定義はGit管理ファイル、学習履歴はIndexedDBに保存する。
- モデルとUIの全体で`single`、`multiple`、`input`形式を扱う。
- v1にバックエンド、認証、クラウドデータベース、必須の有料サービス、必須の外部AI APIを追加しない。
- AI生成問題は、検証と人の承認を通過するまで公開しない。
- 明示的な`schemaVersion`と検証済みマイグレーションにより、バックアップの互換性を維持する。

## 技術規則

- 単純なモジュール、純粋なドメイン関数、TypeScriptのstrictモード、Zodに基づくデータ契約を優先する。
- ストレージ、表示、採点、セッション生成、復習優先度、統計の責務を分離する。
- 本番依存と開発依存のバージョンを`package.json`で固定し、`package-lock.json`をコミットする。
- Node.js 24 LTSを使用する。現在のNode.js 18環境ではアプリを構築しない。
- ユーザーによる無関係な変更を保持し、影響する要件を特定せずに承認済み成果物を書き換えない。
- push、リポジトリ可視性変更、GitHub Pagesデプロイ、リリース公開などの外部変更には、人の明示承認を必要とする。

## 言語規則

- 文書の見出し、本文、表、注記などの自然文は日本語で記述する。
- ソースコード、設定、試験コード、スクリプト内のコメントは日本語で記述する。
- 製品名、ライブラリ名、識別子、型名、API名、コマンド、パス、ファイル名、設定値、データ契約、コード例は、技術的に必要な原表記を維持できる。
- 工程レビューでは文書とコメントが本規則に適合することを確認し、違反は承認依頼前に修正する。

## 検証

現在の工程に存在する関連コマンドをすべて実行する。アプリ基盤作成後の標準ローカル品質ゲートは次のとおりとする。

```text
npm ci
npm run validate
npm run lint
npm run typecheck
npm test
npm run build
```

結合、総合、リリース、ルーティング、PWAに関する変更では`npm run test:e2e`も実行する。アプリ基盤作成前は`git diff --check`を実行し、Markdownリンク、識別子、工程状態、トレーサビリティ、日本語規則への適合を手動確認する。

## 工程引き渡し

各工程の終了時は次の手順に従う。

1. 次の工程を開始せずに停止する。
2. 変更した成果物、判断、検証結果、未解決リスク、承認対象ゲートを報告する。
3. 指摘された修正を同じ工程内で適用し、再検証する。
4. 人の明示承認後に承認台帳を更新し、工程コミットを作成して`main`へ統合し、開発ワークフローで定めた注釈付きゲートタグを追加する。
5. 外部操作についても明示的に承認されていない限り、pushやデプロイを行わない。
